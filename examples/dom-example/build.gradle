apply plugin: 'idea'
apply plugin: 'java'
buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }

    dependencies {
        classpath group: 'org.tlsys', name:'twt-gradle', version:'1.0'
        //classpath project(':twt-g')
    }
}

repositories {
    mavenCentral()
    mavenLocal()
}


dependencies {
    compile group: 'org.tlsys', name:'twt-shared', version:'1.0'
    compile group: 'org.tlsys', name:'twt-rt', version:'1.0'
    compile group: 'org.tlsys', name:'twt-dom', version:'1.0'
}

apply plugin: 'twt-gradle'


classes{
    dependsOn generateTWT
}