apply plugin: 'idea'
apply plugin: 'java'
apply plugin: 'maven-publish'

buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }

    dependencies {
        classpath group: 'org.tlsys', name:'twt-gradle', version:'1.0'
    }
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId 'org.tlsys'
            artifactId 'twt-dom'
            version '1.0'
            from components.java
        }
    }
}

repositories {
    mavenCentral()
    mavenLocal()
}
apply plugin: 'twt-compile'

dependencies{
    compile files("${System.properties['java.home']}/../lib/tools.jar")
    twtCompile name:'twt-shared', group:'org.tlsys', version:'1.0'
    twtCompile project(":rt")
}



build {
    dependsOn publishToMavenLocal
}